/// [ActionType] identifies which type action will be used
// ignore_for_file: public_member_api_docs

enum ActionType {
  navigation,
  state,
  shopify,
  theme,
  revenueCat,
  qonversion,
  stripe,
  firebase,
  firebaseFirestore,
  firebaseStorage,
  supabaseAuth,
  supabaseDatabase,
  camera,
  audioPlayer,
  webview,
  map,
  googleMaps,
  tetaDatabase,
  tetaAuth,
  customFunctions,
  braintree,
  translator,
}
